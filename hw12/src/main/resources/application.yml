spring:
  datasource:
    url: jdbc:h2:mem:maindb

  thymeleaf:
    cache: false


  jpa:
    generate-ddl: false
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none

  liquibase:
    enabled: true
    change-log: liquibase/changelog.master.yml
